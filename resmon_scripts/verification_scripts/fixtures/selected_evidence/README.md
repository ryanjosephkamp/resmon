Authored synthetic documents and output fixtures only; no real papers, corpus, credentials or model calls. Existing exact PDF fixtures are read-only dependencies in the source inventory. illustrative-request.json illustrates canonical hashing/offsets, not a substitute for the complete production contract. The populated schema17 fixture must be authored from frozen base and exercised only after activation; no migration was executed in preparation.
